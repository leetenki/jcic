<!DOCTYPE html>
<html lang="ja">
<head>
  <title>JCIC</title>
  <link rel="shortcut icon" type="image/x-icon" href="/ico.png" />
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <style>
    tr {
        font-size: 15px;
        color: black;
    }
    tr:hover {
        background-color: #dddddd;
        cursor: pointer;
    }
    td {
        padding: 13px;
    }
  </style>
  <script>
    setTimeout("location.reload(true)", 60 * 1000)
  </script>
</head>

<body>
  <div style="text-align:center; margin: 30px 0;">
    <div style="border-radius: 20px; border: solid 2px black; padding: 20px; display: inline-block; font-size: 20px;">
        签证一览
    </div>
  </div>
      <table style="color:#999999; margin: 40px;" class="row">
        <% @result.each do |project| %>
        <tr class="row" onclick="window.open('./detail/<%= project.id %>'); this.style.display = 'none'">
            <td style="border: 1px solid gray" class="col col-md-3 col-sm-3 col-xs-3"><%= project.created_at.strftime("%H:%M") %></td>
            <td style="border: 1px solid gray" class="col col-md-3 col-sm-3 col-xs-3"><%= project.representative_name_chinese %> (共<%= project.total_people %>人)</td>
            <td style="border: 1px solid gray" class="col col-md-6 col-sm-6 col-xs-6"><%= project.china_company_name %></td>
        </tr>
        <% end %>
      </table>
</body>
</html>
